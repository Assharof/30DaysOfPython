#  Day 4 - Strings

##  Topics Covered

* String concatenation
* String methods (`upper`, `lower`, `capitalize`, `title`, `swapcase`)
* String slicing
* Searching in strings (`find`, `index`, `rfind`)
* String formatting
* Escape sequences (`\n`, `\t`)
* Joining and splitting strings



##  What I Learned

Today I worked extensively with strings in Python and learned how to:

* Combine multiple strings into one meaningful sentence
* Transform text using built-in string methods
* Extract parts of a string using slicing
* Search for substrings efficiently
* Format strings for clean and readable output
* Work with escape sequences for better text display



## Exercises Completed

###  String Manipulation

* Concatenated multiple strings into readable sentences
* Converted strings to uppercase and lowercase
* Used `capitalize()`, `title()`, and `swapcase()`

###  String Operations

* Sliced strings to extract specific parts
* Checked for substrings using `find()` and `index()`
* Replaced words in strings using `replace()`
* Split and joined strings

###  Indexing & Searching

* Found positions of characters and words
* Used `rfind()` to locate last occurrences
* Extracted repeated phrases from sentences

###  Boolean Checks

* Checked string start and end using `startswith()` and `endswith()`
* Validated identifiers using `isidentifier()`

###  Formatting & Output

* Used escape sequences (`\n`, `\t`) for structured output
* Applied string formatting for calculations and expressions



##  Sample Highlights

### String Concatenation

```python
' '.join(['Thirty', 'Days', 'Of', 'Python'])
```

### String Formatting

```python
radius = 10
area = 3.14 * radius ** 2
print(f"The area of a circle with radius {radius} is {area} meters square.")
```

### Escape Sequences

```python
print("I am enjoying this challenge.\nI just wonder what is next.")
```


##  Key Takeaway

Strings are one of the most important data types in programming.
Mastering string manipulation is essential for:

* Data processing
* Backend development
* User input handling


##  Progress Update

✅ Day 1 – Python Basics
✅ Day 2 – Variables & Data Types
✅ Day 3 – Operators & Expressions
✅ Day 4 – Strings


##  Next Step

Moving into:

* Conditionals (if/else)
* Logical thinking
* Decision making in programs



*"Every day I code, I get 1% better."* 
